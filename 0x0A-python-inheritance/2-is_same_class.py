#!/usr/bin/python3
"""Function that returns if the objec is an instances of class"""


def is_same_class(obj, a_class):
    """Function that returns if the objec is an instances of class"""
    if isinstance(obj, a_class):
        return True
    else:
        return False
